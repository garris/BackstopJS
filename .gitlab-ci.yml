image: node:$NODEJS

before_script:
  - node -v
  - npm -v

tests:
  script:
    - npm run lint
    - npm run unit-test
  parallel:
    matrix:
      - NODEJS:
          - 14
          - 16
          - 18
